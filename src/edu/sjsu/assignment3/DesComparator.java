package edu.sjsu.assignment3;

import java.util.Comparator;

public class DesComparator implements Comparator<Appointment> {
    @Override
    public int compare(Appointment o1, Appointment o2) {
        //couldnt get this to work so i commented it out
        //TODO: Implement based on requirement
        //description first, then start date, finally end date.
        //if (description.o1 != description.o2) return description.o1 - description.o2;
        //else return startDate.compareTo(o1.startDate, o2.startDate);
        //if (startDate.o1 != startDate.o2) return startDate.o1 - startDate.o2;
        //else return endDate.compareTo(o1.endDate, o2.endDate);
        //else (endDate.o1 != endDate.o2) return endDate.o1 - endDate.o2;

    }
}
